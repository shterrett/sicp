; The procedure should produce an error, and one that is easily corrected by
; reversing the order of the internal definitions. While the ideal
; of simultaneous definition is nice, dependent definitions should be inside
; the scope of the definitions they require. For simultaneous dependance,
; the delay method will suffice.
;
