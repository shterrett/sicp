; When cons-ing the first two elements and then mapping against
; the cdr of the stream, the streams are offset, which allows
; for all the possible combinations.
; However, by skipping the con-sing, the streams are aligned in
; term order and combinations are not realized.
