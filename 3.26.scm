; Assuming keys can be ordered, a key can be structured
; as a list that contains the key, the lesser sub-key, and hte greater sub-key
; If the lesser and greater sub-keys are both '(), then that is the value
